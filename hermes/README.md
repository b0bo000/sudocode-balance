# Hermes 接入

Hermes 不用 Codex plugin。它直接接 MCP。

推荐命令：

```bash
hermes mcp add sudocode-balance --url https://api.sudocode.chat/mcp --auth oauth
```

如果你在无浏览器的服务器环境里，用仓库根目录的 [README](../README.md) 里的服务器 Token 方案。

如果你想手动合并配置，参考 `config.sample.yaml`。
